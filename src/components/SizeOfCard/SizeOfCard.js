import React from 'react'
import './SizeOfCard.scss'

export default function SizeOfCard({changeWidth}) {
    return (
        <  >
            <button className='changeSize' onClick={changeWidth}>S</button>
        </>
    )
}
